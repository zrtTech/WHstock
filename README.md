# Server printing example (Node.js)

This folder contains an **example** implementation for:
- `GET /printers`
- `POST /print-label`
- stubs for `POST /print-a4` and `POST /print-hanger`

It is designed to be merged into your existing Node.js server on Windows.
